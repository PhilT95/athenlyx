# Minecraft Java Server Installation Guide for AlmaLinux 10

In this guide we will setup a Minecraft Java server on an AlmaLinux 10 system and add basic configuration to let the server run automatically as a service on the server.

## Requirements

Before we start we need to make sure that we fulfill the following requirements:

- 